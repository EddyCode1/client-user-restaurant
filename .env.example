# Generado automáticamente por pnpm start:all si no existe.
# Emulador Android: no hace falta cambiar DEV_HOST (usa 10.0.2.2 solo).
# Celular físico: pon la IP de tu Mac (ej. 192.168.1.160).

EXPO_PUBLIC_DEV_HOST=192.168.1.160
EXPO_PUBLIC_API_PORT=3016

# No definas AUTH_URL/API_BASE salvo que sepas lo que haces;
# endpoints.js construye la URL según emulador vs dispositivo.
